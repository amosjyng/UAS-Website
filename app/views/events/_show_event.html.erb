<div class="event_view">
  <h1><%= link_to event.title, event %></h1>
  <span class="event_time"><%= event.human_time %></span>
  <% if summarize %>
    <%= raw event.summary.html_newlines %>
  <% else %>
    <%= raw event.content.html_newlines %>
  <% end %>
  <% if can_edit? %>
    <div class="admin_tools">
      <%= link_to 'Edit', edit_event_path(event), :class => 'admin' %> | 
      <%= link_to 'Delete', event, :method => :delete,
          :data => { :confirm => 'Remove "' + event.title + '"?' },
          :class => 'admin' %>
    </div>
  <% end %>
</div>